# Downloaded tool binaries (auto-fetched on first run)
tools/

# NEVER commit login cookies — they are your live Instagram session!
cookies.txt
*.cookies

# Session logs
logs/

# Local user settings & queue
config.json
urls.txt

# Build artifacts
dist/
build/
*.spec
__pycache__/
*.pyc
*.pyo
*.exe

# Frozen binaries produced by BUILD.sh / BUILD.bat
/MediaGrabber
/MediaGrabber.exe

# Release staging
release/
*.zip

# macOS cruft
.DS_Store

# Extension signing key — fixes the Chrome extension ID. Keep it backed up
# somewhere safe, but never in the repository.
*.pem
/mediagrabber-bridge
/mediagrabber-bridge.exe
